# Unreviewed Codex research snapshot — 2026-09-10

Preserves all tracked modifications, tracked deletions, and nonignored untracked
research files from `/workspace/prism-ba`, based on c28b38d. The original
working directory is untouched. This snapshot has NOT been rebased onto current
master, validated as a single algorithm, or selected as a shipping configuration.
It intentionally includes historical experiments, negative results, absolute
artifact paths, and superseded claims. Read dates, manifests, and retractions.

Start with `bench/ADAPTIVE_MENU.md`, `bench/LAMBDA_HYSTERESIS.md`,
`bench/adaptive_menu_experiment.py`, and `docs/speed_novelty_results.md`.
The convergence_verdict image was moved under `docs/figures/convergence/`.
Build outputs, ignored caches, external BAL inputs, and result directories
outside the repository are not part of this source snapshot.

For the focused performance port use branch `research/iteration-performance`.
For the separately frozen and smoke-tested eta2 candidate use
`research/eta2-champion-publish`, directory `research/eta2_champion`.
Do not merge this WIP branch wholesale into master.

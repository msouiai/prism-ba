# Frozen initialization-noise screen

Four previously specified contrasting BAL instances: Trafalgar126, Dubrovnik88, Final1936, Final13682. Three fixed seeds17,29,43, identical perturbed input file for PRISM and Caspar FP64. One solve per seed/arm:24 pipelines. Seeds are different initializations, not timing repeats or independent scenes.

Use existing perturb_bal.py: independent Gaussian additions to angle-axis coordinates with sigma0.001 radians (about0.057 degrees per coordinate); translation and point coordinate sigma0.001 times the median centered point radius. These are additive angle-axis perturbations, not claimed isotropic SO(3) perturbations. Intrinsics unchanged. Copy all observation bytes verbatim; verify observations, intrinsics, seeded parameters and file hashes before measurement. Preserve original inputs.

Keep the previous expanded screen's medium targets and native caps:4 seconds Trafalgar126 and Dubrovnik88,12 seconds Final1936,20 seconds Final13682. No fresh calibration. Same objective despite altered starting variables. Freeze PRISM v7 early restart + point safeguard and Caspar FP64 default-driver binaries, verified by previous hashes. Same cached CPU audit machinery, with one harness addition allowing an explicit input path. Interleave arms, reverse scene order for seed29, alternate first arm across seeds/scenes. Timeout180 seconds permits large input loading/export.

Keep existing stopping conventions, including PRISM's1e-8 relative inward safety margin. Hits require a certified event within native cap and a CPU-audited final state at or below nominal target. Charge abandoned restart native work. Rank hit count first; finite median speed ratios only when both arms hit all three seeds, within5% descriptive tie. Also report each matched seed; speed across seeds conflates initialization effects with timing variance. Final cost, gap to target, process wall, rejections and restart events retained. Caspar rejection counts reported only if exposed by its existing trace; do not invent them from iteration counts.

No noise-level tuning, retries of failed measurements, extra seeds, observation noise, solver modifications, or additional runs after outcomes. All failures retained. Prior eleven jobs remain paused. Source snapshots, manifests, input checks and endpoint audits accompany results. Compare unperturbed medium results descriptively as historical context; they are not newly interleaved controls. No population-level certainty claim.

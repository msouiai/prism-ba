# Frozen local-census extension, 2026-09-08

Include all 12 local BAL files absent from the preceding nine-scene Caspar FP64 comparison: Ladybug49, Trafalgar126, Dubrovnik88/356, Final871/3068/1936/4585/13682, Venice951/1672/1778. These are additional problem instances, not necessarily independent recordings; BAL family variants can overlap. No exclusion based on performance. No claim of blind sampling or population-wide certainty.

Frozen PRISM v7 early restart with point safeguard and compact FP64 storage; frozen precision-verified Caspar FP64 driver default. Original BAL inputs and objective, independent CPU endpoint checks. Same GPU, serial interleaving, old jobs stay paused. No solver tuning or change of default.

Caps depend only on observation count: <1M 4s, <3M 8s, <6M 12s, otherwise 20s native. Process safety timeout 180s allows loading/export on largest input; it is not a native budget change. One calibration per solver/scene with target1e-100 (effectively unreachable; full cap unless native stopping), exclude these 24 runs from timing comparisons. Reference = minimum independently CPU-audited calibration endpoint from both arms. Targets = reference * {1.02,1.005,1}; freeze all before measurement. This symmetric reference is a budget-dependent benchmark reference, not an optimum. It can favor a lucky pilot trajectory; report misses and actual objective gaps, do not move thresholds.

Three measurement repeats, all 12 scenes and three targets: 216 planned solver pipelines. Seed20260908 permutes scene and level order per repeat; alternate arm order. All abandoned restart native time charged. Calibration failures retained; affected scene/arm marked unavailable in all nine planned measurements, not silently excluded or repeatedly retried. Measurement failures retained, not retried. Both calibration arms failing means scene unresolved.

Rank hit count first per scene/target. When both hit3/3 compare median native target crossing; ratio within1.05 is descriptive tie. Equal incomplete hit counts unresolved; finite speed ratios only both3/3. Report all raw repeats/ranges, endpoint cost, process wall, restart frequency. Do not turn cap-censored times into finite speed ratios. Summarize each level separately and families/scenes equally; do not treat216 repeats or36 scene/target cells as independent scenes. Bootstrap scene-level conditional log-speed summaries only as descriptive stability intervals on this convenience sample, excluding censored cells explicitly and reporting reliability alongside. Family sensitivity and previous nine scenes separately; no pooled headline mixing calibration protocols. No further tuning or adaptive extra runs after outcomes.

Harness correction before valid calibration: target0 was rejected by both drivers before optimization. Invalid invocation logs are retained under `invalid-zero-target/`; these are harness failures, not algorithm failures. Replaced with positive1e-100; no valid optimization outcome informed this correction.

During the measurement startup, CPU audit-input caching was added to avoid repeatedly parsing large text files. Cache keys hash the entire original input; saved arrays are checked bit-for-bit at creation and rehashed at each load, with scorer source hashes checked. Initial cost uses the unchanged scorer; endpoint audit code is unchanged. This work remains outside native and process-wall timing boundaries. All24 completed calibration records and the first six measurement runs are retained without rerunning their solvers. The original measurement plan is archived as `measurement-plan-before-cpu-cache.json`; the only added job field is the CPU cache location. Numeric targets, job order, and all solver parameters remain identical. Solver binaries, target rule, caps, and sample size are unchanged. Cached reload on Ladybug49 reproduced the previous independent endpoint score exactly.

# Preserved research evidence

Large archives are split into sub-45 MiB parts for GitHub. Reconstruct any archive with:

```bash
cat NAME.part-* > NAME
```

Then verify it using the adjacent checksum file. The Eta2 wave-3 directory otherwise preserves its original hierarchy directly.

import pathlib,shutil,subprocess,json,argparse
from build_tr_candidate import sha
ap=argparse.ArgumentParser();ap.add_argument('--output',type=pathlib.Path,default=pathlib.Path('/workspace/prism-tr-preconditioner/fixed'));a=ap.parse_args();root=a.output;root.mkdir();repo=pathlib.Path(__file__).resolve().parents[1];s=pathlib.Path('/workspace/prism-tr-point-prep/combined/source.cu').read_text();parts=[]
for start,end in [('__device__ __forceinline__ void MFVinv(', '// GAP-4090 F2'),('__global__ void MFVinvApply(', '// Map camera traversal'),('template <int CD, class HT>\n__global__ void MFPass2(', '__global__ void MFBackSub'),('template <int CD, typename HT>\n__global__ void MFBlockSchur(', '__global__ void MFMakeEquil(')]:
 a=s.index(start);b=s.index(end,a);parts.append(s[a:b])
(root/'reference.cuh').write_text('\n'.join(parts));shutil.copy2(repo/'bench/fixed_preconditioner.cu',root/'bench.cu');cmd=['nvcc','-O3','-std=c++17','-arch=sm_89',str(root/'bench.cu'),'-o',str(root/'bench'),'-lcublas'];subprocess.run(cmd,check=True);(root/'manifest.json').write_text(json.dumps(dict(command=cmd,source_sha256=sha('/workspace/prism-tr-point-prep/combined/source.cu'),files={p.name:sha(p) for p in root.iterdir() if p.is_file()}),indent=2))

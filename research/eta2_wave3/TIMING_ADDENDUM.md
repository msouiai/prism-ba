# Opening hit screen and final timing isolation

Registered before the opening factorial starts. Its Venice hit screening may
overlap CPU-only lossless compaction of old evidence, with GPU launches still
serialized by the existing lock. These rows select by the pre-registered hit
threshold, never by their potentially affected wall times. Report their walls
as observed, with this limitation. The nine-cell practical timing comparisons
and any selected-policy confirmation wait until compaction and forensic
analysis finish. No reference/control arms, targets or candidate flags change.

Before the cap/opening Final3068 follow-up starts: apply the same separation to
that cohort. Its hit/endpoint results are screening evidence; its observed walls
may include concurrent CPU compaction. Any comparative speed or promotion claim
requires isolated confirmation. This addition does not modify the completed
standalone tail cohort or the registered targets/caps.

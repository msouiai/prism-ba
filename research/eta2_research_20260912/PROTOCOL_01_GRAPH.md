# Covisibility partition control for the coarse-space pretest

The original geometric K8 partition on Final3068 is dominated by one3058-camera cluster and isolated outliers. That concrete partition is not a sufficient test of distributed trajectory modes. Before evaluating a graph partition, register this alternative pretest on all three saved Final3068 witnesses, K8/32/128, N3 arithmetic repetitions. No native performance arm is implied.

Build B from distinct original camera-point incidences, collapsing repeated incidences only for the graph. Set W=B B^T with zero diagonal, exact integer shared-point edge weights. All scored objective observations remain unchanged. Use PyMetis2025.2.2 from the pinned verified wheel, k-way partitioning, unit camera weights, ncuts1,niter10,ufactor30,seed20260912,contig0,numbering0,objtypeCUT,recursive=False. Disconnected components and isolated cameras remain vertices; add no artificial edges. API and dependency provenance were checked on a synthetic bridge graph before witness data.

Keep the original native Sim3 tangent, E scaling, local normalized-column dependence cutoff and missing-direction definition. Report graph membership hashes, cluster sizes, edge cut, cross-cluster original-observation counts, setup wall, rank, total missing-direction fraction, the previous after-global-removal convention and an explicit gauge-free intersection projection. With orthonormal coarse Q and global gauge G, use Q[I-T(T^T T)^dagger T^T]Q^T where T=Q^T G; disclose tolerances and rank. Flag near-zero reference-minus-Eta2 directions, especially Final0, rather than interpreting unstable relative fractions as evidence.

Compare against all retained geometric results, with no relabeling of old negatives. The user's roughly25% coarse-fraction pretest is evaluated only at meaningful nonzero differences; passing it justifies a new proposal, not a native speed claim. Report full-camera fractions as primary (intrinsic rows remain zero), with extrinsic-only fractions explicitly secondary. No state or direction is changed, and no new endpoint or target-hitting claim is produced by this pretest.

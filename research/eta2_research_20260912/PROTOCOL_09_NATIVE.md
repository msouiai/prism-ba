# Native continuation of the passenger coarse correction

The independently globalized three-iteration passenger pretest passed its mechanism gate on two of three Final3068 terminal witnesses, while its maximum full-objective gain was only0.0443%. Its K8 geometric partition isolates a few camera outliers: do not attribute it to distributed low-frequency recovery. This native test answers whether that exact scoped correction helps Eta2 reach targets after a confirmed stall.

Register one runtime-off/on arm derived from the pinned champion. At the first confirmed FTOL stop, after existing backtracking confirmation has had its normal opportunity, schedule one fresh assembly at the unchanged current state and current lambda. Apply the exact three-attempt passenger algorithm of PROTOCOL_09_NONLINEAR.md, with fresh E/Cdiag and the current point trace floor. Freeze its memberships/first-original-observation anchors/centroids and joint metric within that episode. Count all copies, clustering, metric work, coarse Jacobian/normal assembly, small solves, transformations and full scores inside native time. A CPU coarse solve is acceptable if its actual cost is included; do not label CPU wall as GPU time.

Only strict positive full-objective accepted coarse steps are applied. The coarse episode uses its own damping/backtracking already registered; it is not clipped by the fine radius. After any accepted correction, resume fine Eta2 with CURRENT fine lambda/R/numeric floor and forcing history, not restored historical state. Reset only flatness counters to allow another ordinary confirmation interval; mark the one-time intervention spent. If none is accepted, honor the original stop. Reassemble after every state change. No repeated intervention, point-only repair, objective change, or per-scene choice.

Record coarse attempts, accepts, backtracks, episode wall, score_init/full-score parity and controller values before/after. Normal iteration/target logs must include the correction's cost and wall; never report the earlier stop time as crossing after a correction. Independently verify native mapping/Jacobian/metric against the CPU tiny reference, same-cluster invariance and full objective; run ordinary memcheck and N3 original/off compatibility before performance.

Compare same-binary off/on on the common nine cells N3 and Venice/Final3068 N5, normal targets/caps/FTOL. Keep all misses and complete retry/PCG accounting, with probe work separated where it is not a PCG iteration. Park the implementation if target hit counts do not improve on either tail and no disjoint time-to-target benefit appears, or if practical reliability falls or median target time regresses>20%. Small standalone witness gains do not override this native gate.

# One terminal soft-mode perturbation, with continuation

One confirmed-FTOL intervention per solve, after the original backtracking confirmation. Schedule fresh normal assembly at the actual stopped state before deriving any direction. Use K8 geometric coarse basis Z and the actual production damped camera operator Ac=Z^T A_lambda Z; use Mc=Z^T M Z from the current native PCG Cholesky factor. Explicitly remove the seven known global similarity modes in this metric. Solve the generalized small eigenproblem and choose the lowest finite positive gauge-free Ritz mode, with no eigenvalue repair; invalid matrix/rank/energy skips the one intervention. This is a coarse-restricted Rayleigh–Ritz mode, not a certified lowest eigenvector of the complete camera operator.

Complete points homogeneously: dp=-V_lambda^-1 W^T dc, omitting the independent gradient term. Fix the ray's sign so g^T d>=0; exact-zero tie uses largest-magnitude coordinate positive. Set the undamped original GN predicted cost increase to0.1*max(last accepted absolute decrease,1e-12*F) by solving the scalar quadratic in the ray amplitude. Require finite positive ray curvature. There is one proposed perturbation, not a scored menu; accept the temporary move only if its full objective is finite and its true increase is at most twice that budget. It uses this energy budget rather than the old fine radius. Subsequent LM retains CURRENT lambda/R/numeric floor/forcing history, with only flatness counters cleared for one ordinary confirmation interval. No historical controller restoration or cross-iteration Krylov reuse.

Preserve the pre-kick full state. Return the better of that state and the final continued state, including intrinsics and correct cost, so an unrecovered uphill perturbation is not disguised as convergence. Keep the actual nonmonotonic trajectory, restoration-at-return event if needed, and intervention time in the ledger. Any accepted kick and all new work need explicit counters distinct from PCG iterations. The normal target and60s/600-outer budget apply to the entire solve including setup and continuation.

Before timing, validate basis/metric/gauge projection, generalized Ritz residual, homogeneous point response and ray prediction on a tiny system against dense references; validate full-state best restoration and score audits; run ordinary memcheck and N3 original/off compatibility. Preserve all source/header manifests and the common attempt trace. Runtime flag defaults off and no original source/default is changed.

Same-binary off/on nine practical cells N3 plus Venice243740.27 andFinal3068 1744796.9841897595 N5. Report both timing directions, hits, endpoints, score_init, rejects/retrywall, PCG/outer, intervention count/outcome/cost and gauge leakage. The historical Eta2→MFREE portfolio is a contextual comparator (Final3068 observed9/10, median3.84s, cross-machine composed wall), not a fresh matched same-hardware arm. A surviving candidate would require a fresh portfolio comparison before a superiority claim. Park this arm if no target hit or disjoint time-to-target benefit occurs, or it loses practical reliability/time>20%; do not count an immediate uphill kick as a successful basin escape.

# Brief 7: validity audit and one valid full-normal ARC witness screen

Eta2's coupled-damping camera Schur operator and reduced RHS do not form scalar shifts. First verify that obstruction on a small dense coherent normal system: both A(lambda)-A(0)-lambda I and b(lambda)-b(0) must be retained. This invalidates the advertised free zeta-recurrence graft, not cubic regularization itself.

Register a mathematically valid alternative before witness data: work on the complete camera-plus-point normal operator, whitened by the fixed captured positive damping metric D=diag(E^-2,Dp). Include the original intrinsic solve prior. One fresh64-vector fully reorthogonalized Krylov space per witness, built from the full gradient, defines the projected normal operator. No vector is reused across attempts or outers. Solve the projected cubic secular equation lambda=sigma*||y(lambda)|| with a bracketed scalar root to relative1e-8. It uses a fixed matrix and RHS; no shifted proposal is scored as a candidate menu.

One global sigma rule: saved champion lambda divided by the D-norm of its captured step, lower-bounded by1e-30. Report this anchoring and the actual solved lambda. Compare with the projected LM step at the saved lambda using exactly the same64-vector space. Include the captured Eta2 and separately measured coherent reference results as context, not matched timing controls. Use all three full Venice witnesses, N3 CPU repetitions. ARC has its own regularized model and no old-camera-radius clipping; report norm/old radius. Accept only true full-objective decrease, positive undamped GN prediction and rho>.1. The score always uses all original observations.

Before witness runs check the full-normal product and whitening, Krylov orthogonality, and the projected root against an independent tiny dense cubic solve. Store the fresh full-normal residual at each resulting direction; a small projected secular residual is not a certificate of an exact full-space step. Count all64 full products, assembly, orthogonalization, scalar work and full scores. No Schur-product-equivalent or GPU speed claim is inferred from CPU time.

Mechanism gate for implementing this more costly architecture on GPU: ARC gives accepted true decrease exceeding the matched projected LM step by20% on at least2/3 witnesses, without >.15% full-cost regression on the third. Otherwise park this fixed64/sigma-rule formulation. This is a prespecified screening gate, not the supplied nine-cell performance comparison against PI, and not a universal ARC kill. Any promoted continuation must next pay the full-normal storage/work and use the common native protocol.

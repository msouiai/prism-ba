# Brief 9 continuation: actual nonlinear passenger-cluster correction

The first terminal oracle's extra same-fine-radius gate failed. That gate was our implementation choice, not a requirement of the supplied nonlinear coarse brief. Its raw directions gave accepted gains 8.39x and15.59x Eta2 at two witnesses, and the user's zero-decrement kill was not met. Retain that negative constrained experiment; it does not rule out an independently globalized coarse correction.

Before new witness runs, register the actual base-node/passenger formulation on the same three full Final3068 witnesses, N3 CPU repetitions. K8 and deterministic camera memberships use the existing verified geometry code. Assign each point to the cluster of its first observing camera in original observation order; unobserved points stay fixed. Freeze memberships, anchors and cluster centroids during the episode. A cluster similarity transforms its cameras' centers and its assigned points together; camera orientations transform contragrediently. Intrinsics remain fixed, the original scored observations remain intact, and same-cluster reprojections must be invariant. No observation masking or point elimination is substituted for these passenger moves.

Run three coarse GN iterations. Initialize coarse damping from the saved fine lambda. Pull back the original positive camera/point damping metric through the coupled camera-and-passenger tangent at the starting state, keeping that metric fixed for this episode. Use the same dependence threshold as the coarse pretest. Recompute the true full-objective gradient and GN coarse normal at each accepted state, using correct native retraction derivatives. The original intrinsic solve prior contributes zero because no intrinsic is moved. Implementation notes must specify local-versus-fixed-coordinate metric semantics and rank handling before witness results.

Each iteration has one damped GN direction and at most eight dyadic whole-direction backtracking halvings. Acceptance requires positive true full-objective decrease, positive undamped prediction and rho>0.1. A failed iteration multiplies coarse lambda by10; an accepted iteration with rho>.75 multiplies it by.1, otherwise keeps it. This is a separate coarse model with its own cost-based globalization, so it is not clipped by the collapsed fine radius. Report its camera norm divided by that radius. No stored historical fine controller state is restored, and fine lambda/R are not altered by this diagnostic.

Implementation choices frozen before data: centroids stay in their initial world coordinates during three left-increment Sim3 steps, with X'=mu+exp(s)Q(X-mu)+v, the same center action, R'=R Q^T and t'=-R'C'. The fixed metric is M0=Bc^T diag(E^-2)Bc+Bp^T Dp Bp, with Dp equal to the actual captured point penalty divided by captured lambda, including its trace floor. Retain all seven local modes subject to the JOINT camera-plus-point metric rank, using column-normalized stacked sqrt(D)B SVD at1e-10 and fixed metric-whitened coordinates; do not reuse camera-only rank50, because singleton-camera scale may move passengers. There are three solve attempts total, each scoring alpha1 then up to eight halvings. Use the undamped GN pred(alpha). Analytically zero same-cluster Jacobian rows follow exact Sim3 invariance and must be checked against the full chain rule and finite differences; those observations still remain in every full-objective score.

Before the grid verify same-cluster invariance, finite-difference derivatives and a tiny dense reference. Stream/sparsely accumulate the Jacobian; avoid a dense observation-by56 array. Retain per-iteration prediction, gain, rho, damping, backtracks, point displacement, score_init, operator work and complete CPU wall, including assembly. Compare cumulative gain with the stored Eta2 direction and with the earlier eliminated-point coarse direction, labeling their different models.

Mechanism gate for a native continuation: accepted cumulative gain exceeds twice the stored Eta2 gain on at least2/3 witnesses. Otherwise close this passenger formulation. This gate is only a diagnostic screen: fixed-state N3 repetitions are not independent trajectory repetitions, a multiplier on a tiny gain is not a >.15% endpoint win, and CPU timings are not GPU speed evidence. Any native rollout needs a separate frozen global trigger and the common identical-target panel.

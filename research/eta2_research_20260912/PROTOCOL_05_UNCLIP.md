# One attribution arm: no radial clipping in the first three accepted outers

Registered after the accurate-opening full grid and its trace audit, before any attribution runs. The full arm changes three ingredients; this arm changes only the opening radial-clipping shortcut. For n_accept<3 retain the computed production camera step without radial clipping, while retaining the ordinary strict radius admissibility test, full cost/rho checks, safeguards, backtracking and retry policy. Thus an oversized proposal is rejected rather than automatically accepted outside the ball. First-radius initialization remains normal. After accepted step3 all behavior is frozen Eta2. Keep original production mixed storage, original PCG forcing, caps and normal point completion throughout.

The motivating existing Venice trace: both arms reach almost the same cost after two accepted steps, then the third step is clipped-and-accepted off versus rejected-and-retried on. The latter hands over at much larger lambda and smaller radius despite a worse three-step cost. This is a hypothesis from old data, not an outcome of the new arm. Use the same global rule everywhere; no scene classification, lambda reset, historical restoration, learned rule or candidate menu.

New isolated binary/overlay, runtime off/on, reverse-patch frozen-source check, ordinary tiny memcheck, then N3 original-versus-derived-off compatibility. Keep the same lightweight attempt trace and preserve original source/44 headers. Do not overwrite the completed full-frontload binary or its results.

Run the common nine practical cells N3 and Venice52/Final3068 tails N5, plus the same Ladybug1197 target369997.000889023 N5. Targets, 5/5/8s practical caps and60s/600-outer tails, normal FTOL, independent endpoint audit, alternating order and all work accounting remain as PROTOCOL_05_NATIVE.md. A local Venice benefit does not authorize selecting this flag only for Venice. Promotion requires no lost practical target reliability, no >20% practical median-time regression, and a repeatable tail benefit without a material regression on the other tails. Report every crossing direction and reject/retry/PCG counters.

# Brief 5: one front-loaded accuracy arm

The racing predictor failed; front-loaded linear accuracy is a separate user-proposed hypothesis. Register one setting before its native runs: for the first three accepted outers, solve the coherent FP64 Jacobian-based Schur system to relative residual0.05 using the existing camera-block preconditioner. Recompute its reduced RHS and conditional point completion from those same Jacobian rows and the current coupled lambda. No Krylov reuse, learned rule, alternate lambda menu, source/default edits or scored-objective change. Thereafter return to the ordinary frozen Eta2 path, carrying the normal controller history forward.

During those opening attempts, do not radially clip a fully computed camera direction. Retain the original radius admissibility check and true-cost/rho acceptance; an oversized raw proposal is rejected and follows the usual damping/radius retry policy. The first radius is initialized from the raw opening direction as usual. This makes 'no clipping shortcut' explicit without silently accepting outside the registered ball. Backtracking and per-track old/full safeguards remain available, with their actual full model recomputed. Keep existing numerical repair; a coherent positive operator is not a proof that every finite arithmetic product passes its cutoff.

Use the same coherent-row construction already validated in Brief0, but integrate it into the native loop rather than calling the diagnostic's 20,000-step exact-solve driver. Limit to the ordinary native CG cap and charge row creation, point factorization, allocation, RHS, products, scoring, transfers and destruction. Store actual recursive/fresh residual and opening work counters. Recheck the linear system and point completion on the tiny fixture; run sanitizer and N3 original/derived-off compatibility before timing. The default-off source must recover the frozen source by reversing substitutions.

Compare same-binary off/on on the common nine practical cells N3 and Venice/Final3068 targets N5. Also compare Ladybug1197 N5 at target369997.000889023, fixed as1.01 times the separately collected frozen-champion N20 endpoint median366333.66424655734. Native cap60 seconds/600 outers with normal FTOL on tail cells. Retain all endpoint and score_init audits, misses, both signs of change, retries/PCG and all overhead. Do not merge the calibration cohort into measured results.

Kill if no tail target benefit with disjoint timing ranges or improved observed hit count appears, or if a practical median-time regression exceeds20% or sampled target reliability falls. These N5 counts screen a configuration, not population certainty. Do not promote from changed quality alone while ignoring opening work. This test combines tighter accuracy, coherent storage/products and unclipped opening proposals; any positive attribution would require separating those ingredients afterwards.

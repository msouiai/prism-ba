# Brief 10 registration: separable rescue at Final3068 witnesses

Use only the three saved Final3068 terminal states and their saved Eta2 camera/point direction. These are proposals before the later native point safeguard; the correct baseline is therefore a reconstructed **binary per-track {0,1} selection**, not the unguarded stored step. Evaluate every original observation at the proposed cameras. Retain full movement on exact ties, matching the existing strict-keep comparison.

Three arms, in order: (A) that binary control; (B) per-track alpha in {0,1/4,1/2,1}, preferring larger alpha on exact ties; (C) binary point selection followed by independent keep/move camera selection at those fixed chosen points, retaining moved cameras on exact ties. No repeated alternation, per-scene tuning or nonlinear retriangulation. N3 CPU repetitions each, all3 witnesses (27 runs).

For B, full-step inclusion guarantees the fixed-camera objective is no worse than A, and alpha<=1 cannot exceed that source point's Euclidean displacement. For C, objective separability across unshared cameras gives the analogous no-worse guarantee. Recompute the original full GN prediction and strict rho>0.1 acceptance for the actual masked direction: true-cost monotonicity relative to A alone does not guarantee acceptance or useful outer progress. The original radius uses diagonal camera scaling, so zeroing whole camera blocks does not increase it.

Check both guarantees against direct full scoring, score_init parity, k2 masks, selected fractions/cameras, actual true decrease, rho, cheirality and displacement, and CPU work. Kill an arm if it rescues no additional previously rejected binary-control witness. Also report any local decrease gain, but do not call that a native speed result or use the already-present point safeguard as new work. If neither arm survives, no native rollout. A combination would require an independently justified and registered continuation. Earlier periodic retriangulation and candidate menus remain excluded.

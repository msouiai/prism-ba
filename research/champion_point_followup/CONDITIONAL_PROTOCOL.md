# Conditional follow-up, registered after the primary screen

The complete 72-run native screen found no promotion; every overlay regressed
Ladybug49. Dubrovnik polishing cut 10 outers to 6 but extra evaluation cost
made it slower. Venice basins differ even for the unchanged champion. Fresh CPU
low-parallax seeds retained the specialized polish (1.369x) and ray (1.169x)
effects. These observations motivate one additional, explicitly exploratory
controller; this is not a reclassification of the primary experiment.

Use the already computed full-GN prediction to trigger a point overlay only if
the original candidate rho <= .25 or its prediction is invalid. This uses the
existing trust-region contraction threshold, not a fitted classifier. Permit
rescue of the last failed candidate as well as improvement of a saved one.
Polish all tracks or apply virtual rays; retain the champion's original forcing
and point safeguard. Native point acceptance and camera-radius guards remain.
N=3 against the same frozen targets on the same three scenes; reuse baseline
screen rows only for exploratory comparisons. No larger-scene promotion without
new matched repetitions meeting the original gate.

Also audit disabled flags against the frozen binary at 8 outers with N=6 on
Dubrovnik88. The first pair differed by 3.98e-6 relative with identical outers,
rejects and matvecs, exceeding an overly strict initial 1e-6 parity assertion.
Record that failed assertion and examine original-binary repeat variability;
do not silently relax it or claim bitwise parity. If ambiguity persists,
use original-vs-generated fixed-target N=10 on Venice52 to expose basin effects.
All original screen traces/binary hashes remain archived.

The conditional N=3 Venice rows gave polish 3/3 hits versus 2/3 for its matched
champion, without a clear median speed win. Extend these TWO arms to N=10,
rotating order, unchanged target and flags. This is a reliability/variability
check prompted by an apparent hit-rate difference; do not treat 3/3 as proof.

Final primary conditional Venice counts are 10/10 versus 6/10; the separate
original-binary audit was 7/10. Successful-run medians favor Eta2. Because the
user accepts sub-percent objective differences and values speed, perform a
secondary target-sensitivity check at 1.005 and 1.01 times the SAME frozen
reference endpoint (not candidate-derived endpoints), N=3 per arm and target,
Venice52 only, champion vs conditional polish. This is post-screen sensitivity,
not a new promotion target. Report both tolerances regardless of outcome.

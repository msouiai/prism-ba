# Follow-up: observe a genuine collapse handover

The complete small opening-cap screen produced no collapse-triggered handover;
all observed handovers hit the eight-accepted-step safety limit. Thus that
screen cannot validate the proposed collapse boundary. Preserve it and the
already registered N=10 bimodal extension without modification.

After those timed runs finish, make a separate diagnostic binary exposing only
the opening safety limit. Keep defaults at eight for reproducibility, and run
with the limit set to the existing 600-outer cap: only the registered two-
accepted-menu collapse rule can then hand over early. Same three small scenes,
same fixed targets and 12-second native cap, N=3. Compare a fresh same-binary
champion with the already selected menu-feedback arm. No threshold tuning,
no new large scene, no storm re-entry, no stopping-policy change. Report all
misses and whether the collapse event ever occurs. This is a post-screen
mechanism diagnostic, not a replacement primary study or a promotion gate.

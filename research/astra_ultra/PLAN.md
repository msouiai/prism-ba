# Completed ultra-effort sequence

1. Spawned requested gpt-6-astra adviser at ultra, with exact user prompt and
   previous evidence. Stored independent proposed derivations and sources.
2. Froze new view-diverse cases, feasible references, tight primary/secondary
   targets and shared gauge/objective/timing contract before candidate outcomes.
3. Registered, implemented and checked virtual-ray/moving-host retractions.
   Completed180full CPU comparisons. Virtual rays improve low parallax but the
   observed-polish control is stronger; no cross-family promotion.
4. Completed432frozen ray evaluations to explain finite joint-model changes.
   The undamped path is a diagnostic only, not a tuned convergence arm.
5. Registered/implemented the perspective-stiffness factor and trace control.
   Verified36parents and12initial finite-difference checks; prevalence gate fails.
   No full stiffness/extra-damping comparison after that conditional stop.
6. Registered/implemented Schur point-credit stopping. Verified45systems and
   315chosen proposals; the cheap bound fails its opportunity gate. No native
   or full-PCG trajectory expansion based on expensive oracle/control wins.
7. Adviser independently reviewed all three outcomes and implementation scopes.
8. Saved report, complete data, plots, endpoints, checks and SHA256 inventory.

Current production winner remains frozen Eta2. All original code is preserved.
No current result is a new Caspar comparison, GPU speed claim or novelty proof.

# Exact native healthy directions for O3/O4 locality gates

Registered before collection. The old healthy Schur captures lack the final
joint point step. Do not infer native detector locality from a coherent
recompletion alone. Capture N=3 fresh off-policy Ladybug539 runs at the
registered 1.005 practical target using the already verified wave-3 E4 capture
binary. This is diagnostic timing, excluded from the scored panel.

Retain complete state, actual accepted direction and available attempt metadata
for the first three accepts (or all accepts if fewer). Preserve all capture
metadata and source hashes; other NEW intermediate arrays may be omitted after
the selection. A hash is not a replacement for an omitted state. All final
endpoints remain durable and independently rescored. Analyze every selected
state for O3 attribution and both denominator caps, and O4 sensitivity trigger.
The same detector thresholds in PROTOCOL.md apply; no tuning after collection.

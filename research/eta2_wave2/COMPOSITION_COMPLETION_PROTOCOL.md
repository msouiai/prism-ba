# Completion of the registered W1 evidence set

These are diagnostic runs, never solver speed rows. Capture the full pre-clipping
state and raw direction at every attempted solve on Venice52, off and coherent
accurate opening, N3. Keep original target243740.27 and600outers, but allow300s
to accommodate snapshot IO; all IO is charged and all diagnostic wall is labelled.
Postprocess every finite solve with the registered K1/K8 basis, point-only model
offset and symmetric cross-term allocation. No trajectory is selected for its
endpoint. N3 are fresh diagnostics, not replacements for the scored W3 cohorts.

Also reconstruct the next native solve at ALL five original static-track-damping
Venice terminal endpoints (tail-results.json, on, reps0–4). Use each recorded
post-update lambda/radius, same1,1,0.3 point-factor rule, and the original endpoint
matrix state. Three repeats per endpoint. The old forcing-controller RHS norm
and exact previous attempt state are unavailable: use the native fresh-run eta
and report it. These are NEXT-SOLVE reconstructions, not recovered historical
425-ratio directions. Stop immediately after raw-step capture; no new direction
is committed. This exception replays the already tested static arm for diagnosis;
it does not reintroduce static damping as a prospective algorithm.

Default-off collector, frozen source/config checked, original-v-off replay audited
through identical initial score and independent end-state score where exported.
Point completion/model decomposition uses coherent CPU rows at captured tau and
the captured guarded point diagonals, including the static class multiplier only
for reconstructed static states. Norm fractions and model allocations are distinct.
Large exports live transiently in RAM, then a lossless archive with member hashes
is verified before raw files are removed. Reports and provenance remain durable.

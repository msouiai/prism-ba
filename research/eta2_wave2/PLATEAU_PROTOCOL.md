# W8: bounded root-then-geodesic stopping intervention

Registered after global W2/W5 screens and before plateau scores. Test late-only
use without altering the opening. At the first actual champion stop (after its
backtrack confirmation) above target, keep CURRENT state/lambda/radius: one
logical outer uses coupled W2 root fitting (kappa2, maximum8 root updates), then
two logical outers use analytic W5. Retries do not spend logical outers. Treat
high-ratio flatness as a reason for bounded escalation, not unlimited removal
of the stop rule. No restoration, restart, noise or initial-state change.

If cumulative relative progress from that stop exceeds1e-4, resume the original
algorithm with the intervention permanently spent. Otherwise stop after the
three trial outers. Target crossings end normally. Log the original stop
cost/time plus all subsequent work and outcome. This within-run boundary gives
an exact pre-intervention miss witness; keep it separate from independent A/B
cohorts. No claim that three outers exhaust the nonlinear problem.

One derived binary, off/on, frozen objective/flags, N3 original-v-off parity.
Venice52/Final3068 N5 per arm at243740.27/1744796.9841897595,
600outers/60native seconds. If the Final on-runs supply fewer than five above-
target stop witnesses, draw further on-runs until five witnesses or30total Final
on-runs. Retain EVERY extra fast success and miss; do not use the selected five
as an independent success-rate denominator. Apply the requested0/5 witness
rescue kill before a practical grid. A multi-part winner needs root-only and
geodesic-only isolation before promotion.

The new derivative keeps root lambda above the current numerical floor, including
same-outer changes. Historical root binary/rows unchanged (zero floor violations
in their audit). Allocate optional geodesic workspace only when its phase starts;
charge allocation and telemetry. No fresh MFREE/Caspar timing claim.

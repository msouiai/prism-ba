# B6v7 profile protocol

Registered 2026-09-14 before collection. Profile current unordered compile-time-FP32 compact2 B6v7 on Muell-gba146 and the largest feasible BAL scene in order Final13682, Final4585, Final1936. Import and hash the archived B6v7 generated source, build recipe, and full flag overlay; the archived `optimized_candidate.json` reference SHA is `53ba1cae00a42d3e84f40250a9a2362e5f8e58000157637c4d8050af2689961a`. A frozen-champion-only build is invalid for this stage. Record binary/input/config hashes and effective flags. Use `/tmp/prism_gpu.lock`.

Pinned native configuration is the champion manifest flags and CLI (`lam0=0.1`, `max_iter=600`) with the existing fixed scene target/cap registrations; record both post-setup CSV crossing and full returned solve wall and never conflate them. Use >=3 unprofiled runs for timing and independent FP64 endpoint rescoring. Profiled runs explain phase shares/call counts only. Use captured identical states/work for kernel attribution. Attribute setup, assembly, Pass1, point solve, Pass2, BLAS/reductions, scoring and synchronization where observable, and name the largest measured ceiling without cross-scene extrapolation.

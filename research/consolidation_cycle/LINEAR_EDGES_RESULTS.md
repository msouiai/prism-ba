# Linear-edge extraction results

The clean derivative is based on production commit `d3d42dcb803f104424a3343364cac414a7ab7342` and frozen source SHA `22c18359a526526ae0a5b5109187878ab1cef33b453f6bbddc2aa92597a607d8`. It adds only the default-off `OCA_AUDIT_LINEAR_EDGES` behavior. Synthetic RHS injection is absent from the rebuilt production binary `422ba229969fe3b466edc97baff5e89a0cb7299864ffe9c2b91c8447c47361f6` and enabled only by the test-only compile define `PRISM_LINEAR_EDGE_TEST_FIXTURE`.

The CPU suite passed zero, ordinary, positive-subnormal-square (`1.6e-162`), underflow, overflow, mixed-scale, nonfinite, forcing-ratio, and exact Schur counterexample cases. The fixture build SHA was `be0d3374b4160305009bce20e4fd81c67a35ad3bb3e98f68b583bd0e9a5d5347`. Its Ladybug49 injected-RHS run recorded 90 depth-zero events, zero Schur products, zero negative-curvature events, and 18 accepted point-only updates. Independent FP64 endpoint cost was `48246.89873233431`, down from `850912.4606413026`; native/audit relative error was `4.37e-15`.

The direct frozen-versus-derived flag-off diagnostic failed exact equality in costs, decisions, and endpoint. Both arms used the unordered atomic production path, and their initial costs already differ, so this is expected run-to-run nondeterminism rather than evidence of a math regression. A subsequent current-source comparison placed both arms on the identical archived B6v7 fixed-order substrate. Baseline, current flag-off, and current flag-on ordinary Ladybug49 runs matched exactly in accepted costs, decisions, endpoint SHA, iterations, final cost, accepts, rejects, Schur products, and negative-curvature count. `FIXED_PARITY_RESULTS.json` therefore closes the registered exact compatibility gate for the current always-scaled implementation.

The enabled robust norm copies the reduced RHS to the host on every solve attempt. This is a correctness path, not a speed candidate. Enabled error paths retain the solver's pre-existing manual-allocation cleanup limitations; full solver exception-safe ownership has not been validated.

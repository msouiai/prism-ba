# Static long-track damping transfer, registered before outcomes

The frozen Eta2 source and 44 headers are verified by its builder. Same champion flags, original observations, full L2 SIMPLE_RADIAL, unshared intrinsics and k2=0. No accurate-opening arm, changed stopping rule, feedback controller, scene-specific tuning or extra candidate menu.

One fixed global rule: tau_j = lambda for m_j <= 5; tau_j = 0.3 lambda for m_j >= 6, applied on every attempt, including retries. This is the supplied three-class rule (2 / 3–5 / 6+ with factors 1 / 1 / 0.3); tracks of length below two also retain factor 1. Multiply the complete effective point damping after the original scalar coupling. Preserve the existing per-coordinate floor and all camera damping, numerical repairs, trust radius and acceptance logic. Point factors consistently feed the Schur products, reduced RHS, scaling and point back-substitution. Static track membership makes the existing unchanged-tau retry-cache key valid within a run.

Implement in the existing split point-factor kernel with two CSR-offset reads and one multiplication, no extra launch, global buffer or candidate solve. Do not claim literally zero overhead. Configuration guard rejects other point-floor/split/precision modes instead of silently changing them. Off mode calls the original kernel.

Correctness gates: inverse source substitutions reproduce the frozen source; FP64 augmented-factor energy matches V + tau_j D on synthetic blocks at lengths spanning all three classes; thin-only full toy off/on identity; long-track toy plus compute-sanitizer; N=3 original-versus-derived-off Dubrovnik88 compatibility (median objective difference <=0.15%). Track length counts and initial score parity recorded independently.

First screen: Venice52 target 243740.27 and Final3068 target 1744796.9841897595, N=5 per arm, 60 native seconds / 600 outers and ordinary champion FTOL. Alternating arm and scene order. All endpoints CPU-audited, complete curves/attempt traces retained. Report hits, censored first-crossing times, final cost, outers, rejects, retry/failure wall fractions, PCG/outer and score_init. N=5 hit differences are screening evidence, not population reliability.

If at least one scene gains two or more observed hits without losing two on the other, or endpoint median improves >0.15% without another endpoint worsening >0.5%, proceed to the frozen nine-cell practical panel (N=3) and N=5 endpoint guards Dubrovnik356 and Final4585. Otherwise stop this dose as a negative transfer screen; retain both signs and do not sweep doses. Any successful-screen continuation must be registered before running it. No global promotion from two scenes alone. A useful arm must also preserve practical target reliability and avoid >20% median-time regression. A full panel is not a requirement for a failed initial transfer screen.

Other solver's reported numbers are external supplied evidence, not independently replicated here. The mathematical opportunity is real, but transfer is empirical: reducing point regularization changes both the camera Schur complement and its right-hand side, which interact with Eta2's trajectory and clipping. Track count is not a proof of geometric conditioning.
